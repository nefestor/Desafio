import { NumerosInputDirective } from './numeros-input.directive';

describe('NumerosInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NumerosInputDirective();
    expect(directive).toBeTruthy();
  });
});
